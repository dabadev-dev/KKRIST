
export type PropsCustomer = {
  image: string;
  name: string;
  feedback: string;
  subtitle: string;
};

export type MenuList = {
  name: string;
  url: string;
};

export type PropsCard = {
  image: string;
  title: string;
  buttonText: string;
};

export type PropsBestseller = {
  image: string;
  title: string;
  title2: string;
  price: string;
};

export type PropsStory = {
  Image: string;
  icon: string;
  text: string;
  description: string;
}

export type PropsProduct = {
  image: string;
  title: string;
  title2: string;
  price: string;
};

export type PropsColor = {
  color: string;
  name: string;
  count: number;
}

export type PropsItem = {
  url: string;
  icon: string;
  name: string;

}